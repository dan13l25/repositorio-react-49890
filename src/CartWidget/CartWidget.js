import cart from "../img/carrito.png"

const CartWidget = ()=>{
    return (
        <div>
            <img src={cart} alt = "carrito de compra" />
            0
        </div>
    )
}

export default CartWidget 